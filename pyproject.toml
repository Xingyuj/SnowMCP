[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "servicenowautomation-mcp"
version = "0.9.0"
description = "ServiceNowAutomation FastMCP tools for ServiceNow Knowledge retrieval"
requires-python = ">=3.12"
dependencies = [
  "azure-monitor-opentelemetry>=1.8.7,<2",
  "fastmcp>=3,<4",
  "httpx>=0.27,<1",
  "pydantic>=2.7,<3",
  "pydantic-settings>=2.2,<3",
  "truststore>=0.10,<1",
  "uvicorn[standard]>=0.27,<1",
]

[project.optional-dependencies]
dev = [
  "pytest>=8",
  "pytest-asyncio>=0.23",
  "pytest-cov>=5",
  "respx>=0.21",
  "ruff>=0.6",
  "mypy>=1.11",
]

[project.scripts]
servicenowautomation-mcp = "servicenowautomation_mcp.server:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.mypy]
python_version = "3.12"
strict = true
packages = ["servicenowautomation_mcp"]
